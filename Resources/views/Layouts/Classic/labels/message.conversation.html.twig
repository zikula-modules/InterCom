<div class="conversation-list row">
    <div class="col-md-12">
        {% for reply in message.conversation %}
            {{ include('@ZikulaIntercomModule/Layouts/Classic/labels/conversation.reply.html.twig', {'message': reply }) }}
        {% else %}             
        {% endfor %}
    </div>
</div>