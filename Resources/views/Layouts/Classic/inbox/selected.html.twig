<div class="list-selected list-group-item list-group-item-warning clearfix">
    <div class="col-md-12">
        {{ __('You have selected') }} <span class="selected-messages-count">0</span> {{ __('messages') }}                     
        <div class="multi-actions btn-group btn-group-sm">  
            <button type="button" data-action="delete-selected" class="multi-delete-selected btn text-danger btn-default" title="{{ __('Delete selected') }}"> {{ __('Delete selected') }} <i class='fa fa-trash'></i> </button>
            <button type="button" data-action="markread-selected" class="multi-read-selected btn text-default btn-default" title="{{ __('Mark selected as read') }}">{{ __('Mark selected as read') }} <i class='fa fa-eye'></i> </button>
            <button type="button" data-action="save-selected" class="multi-save-selected btn text-success btn-default" title="{{ __('Save selected') }}">{{ __('Save selected') }} <i class='fa fa-save'></i> </button>
            <button type="button" data-action="unselect-selected" class="multi-unselect btn text-default btn-default" title="{{ __('Un select') }}">{{ __('Un select') }} <i class='fa fa-square'></i> </button>
        </div>
    </div>
</div>