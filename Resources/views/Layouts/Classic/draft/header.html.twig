<div class="list-header list-group-item list-group-item-info clearfix">
    <div class="row">
        <div class="col-md-2">
            {{ __('Created') }}
            {%  if filter.sortby == 'created' %}
                {%  if filter.sortorder == 'ASC' %}
                    <a class="header-action orderby-created" href="{{ path('zikulaintercommodule_messages_getmessages', {'box': box, page:pager.page, 'sortby':'created', 'sortorder':'DESC'}) }}"><i class='fa fa-sort-desc'></i></a>
                    {% else %}
                    <a class="header-action orderby-created" href="{{ path('zikulaintercommodule_messages_getmessages', {'box': box, page:pager.page, 'sortby':'created', 'sortorder':'ASC'}) }}"><i class='fa fa-sort-asc'></i></a>
                    {% endif %}
                {% else %}
                <a class="header-action orderby-created" href="{{ path('zikulaintercommodule_messages_getmessages', {'box': box, page:pager.page, 'sortby':'created', 'sortorder':'ASC'}) }}"><i class='fa fa-sort'></i></a>
                {% endif %}
        </div>
        <div class="col-md-4">
            {{ __('Subject') }}
            {%  if filter.sortby == 'subject' %}
                {%  if filter.sortorder == 'ASC' %}
                    <a class="header-action orderby-subject" href="{{ path('zikulaintercommodule_messages_getmessages', {'box': box, page:pager.page, 'sortby':'subject', 'sortorder':'DESC'}) }}"><i class='fa fa-sort-desc'></i></a>
                    {% else %}
                    <a class="header-action orderby-subject" href="{{ path('zikulaintercommodule_messages_getmessages', {'box': box, page:pager.page, 'sortby':'subject', 'sortorder':'ASC'}) }}"><i class='fa fa-sort-asc'></i></a>
                    {% endif %}
                {% else %}
                <a class="header-action orderby-subject" href="{{ path('zikulaintercommodule_messages_getmessages', {'box': box, page:pager.page, 'sortby':'subject', 'sortorder':'ASC'}) }}"><i class='fa fa-sort'></i></a>
                {% endif %}
        </div> 
        <div class="col-md-2">
            {{ __('Recipients') }}
        </div>
        <div class="col-md-3 text-right pull-right">
            <div class="btn-group btn-group-sm pull-right">
                <a type="button" data-action="toggle-select" class="multi-toggle-select btn text-default hide" title="{{ __('Select all') }}"><i class='fa fa-check-square'></i> </a>
            </div>
        </div>
    </div>
</div>